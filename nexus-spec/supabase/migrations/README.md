# Migraciones pendientes de implementación
Generar migración inicial a partir de specs/data-model.md: projects, catalog_items, requirements, match_proposals, selections, selection_items, change_requests, activity_log. Incluir FK/unique/checks, índices por selección y ticket.status, RPC transaccionales y guardas.
No se entrega SQL aplicado ni una app implementada. Probar rollback, aceptación repetida, conflictos y finalización contra datos reales de desarrollo antes de afirmar integración terminada.
